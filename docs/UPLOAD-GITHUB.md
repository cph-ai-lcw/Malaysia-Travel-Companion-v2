# 上傳到 GitHub Desktop

1. 解壓縮本專案。
2. 將解壓縮後「資料夾內的全部內容」複製到本機 Repository：
   `桌面/GitProjects/Malaysia-Travel-Companion-v2/`
3. 如出現同名檔案，選擇覆蓋。
4. 開啟 GitHub Desktop。
5. Summary 輸入：
   `Milestone 1 Complete Project`
6. 按 `Commit to main`。
7. 按 `Push origin`。
8. 到 GitHub → Settings → Pages，選擇 main / root。
